# Third-party notices

This repository contains original project code under [MIT License](../LICENSE).

## Included assets

- Be Vietnam Pro font: [SIL Open Font License 1.1](../assets/fonts/OFL.txt).

## Build dependencies

Flutter, Dart, Android Gradle Plugin, Kotlin, AndroidX and Dart packages are
resolved from their upstream package registries. Their notices remain in their
respective distributions and, where applicable, in Flutter-generated APK
notices.

## Gradle Wrapper

`android/gradlew`, `android/gradlew.bat`, and
`android/gradle/wrapper/gradle-wrapper.jar` are Gradle Wrapper files from the
Gradle distribution. They are licensed under Apache License 2.0. The wrapper
downloads the Gradle version and SHA-256 pinned in
`android/gradle/wrapper/gradle-wrapper.properties`.

## Embedded Node runtime

`android/app/libnode/` is a local build input and is intentionally not tracked
in this repository. Anyone distributing an APK with that runtime must obtain it
from its upstream source and include every upstream license and notice required
for the exact Node build and native dependencies shipped.

Do not claim MIT coverage for code or binaries from third parties.