# Phase 5: Android Verification Scripts

## Completed

- Added `scripts/verify_android.ps1` for doctor, analyze, tests, and optional debug APK build.
- Added `scripts/build_debug_apk.ps1` for quick APK generation.
- Documented the verification script in `README.md`.

## Notes

- Scripts assume Flutter is installed at `C:\tools\flutter` and Android SDK at `C:\AndroidSDK`.
- APK artifacts remain ignored and are not committed.
